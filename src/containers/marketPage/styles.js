import { StyleSheet } from 'react-native';
import { GREY_BLACK } from '../../helper/constants';

const styles = StyleSheet.create({
  mainContainer: {
    backgroundColor: GREY_BLACK,
    flex: 1,
  },
});

export default styles;
